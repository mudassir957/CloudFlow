apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-orders-pvc
  namespace: cloudflow
spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: cloudflow-local

  resources:
    requests:
      storage: 2Gi